[← Roadmap](../ROADMAP.md)

---

# TKT-031: Migrate documentation to .docs/wiki and fix generator

## Description
Rename `.docs/claude` to `.docs/wiki` across the repository to reflect that it is no longer editor-specific. Update links in `README.md` and script paths.
Fix `build_project_wiki.py` so it preserves all existing `ADR-`, `EPC-`, `FND-`, and `TKT-` markdown files, instead of blindly deleting them.

## Acceptance Criteria
- [ ] Directory `.docs/claude` renamed to `.docs/wiki`
- [ ] Root `README.md` links updated
- [ ] `build_project_wiki.py` references updated and `PRESERVE` logic fixed
- [ ] No data loss on wiki generation
